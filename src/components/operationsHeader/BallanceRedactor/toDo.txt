Спасибо команде было приятно работать)
Задачи: 
    1.Внесение баланса
    2. Кнопка перехода на страницу статистики

    Все мои компоненты реализованы с применением Хуков. потомучто это просто, приятно и полезно)

Возможности бэка: 
    1. Только добавить баланс.

Реализовано: 
    1. Добавление первоначального баланса.

    2. Возможность изменять баланс в любую сторону как в + так и в -. 

       Реализовано через обход логики бэка путем добавления в стейт отрицательного баланса.

    2.1 автофокусировка при клике

    3. проверка на ввод то что введены только цифры

    4. отработка  Esc 

    5. отработка что не пуская строка те если сабмитим ничего то ретёрн

    6. Реальзация отображения текущей валюты приложения и ИЗМЕНЕНИЯ баланса уже в текущей валюте приложения!!!

    Из библиотек посльзовался только moment для даты. 
    больше по задаче ничео такого не было чтобы юзать библиотеки

Хотелось но не удалось: 
    1. чтобы при изменении баланса в другой валюте в  инпут приходило цифра именно в выбранной валюте. Сейчас показывается только в гривне. чне не есть приятно.
    Для этого нужно было по другому изначально писать логику изменеиня валюты приложения. Но тк это было вообще незапалнированным допом  уже оставили как есть)

    2. пытался подключить библиотеку Формик но оказалось что там нет возможности делать кастомную валидацию. поэтому написал и оставил в стороне)



Фильтр категорий: 

Задачи:
    1. Сделать отображение категорий расходов.
    2. чтобы было красивенько и работало)

Реализовано: 
    1. Собственно поставенаня задача реализована с волной мере и небольшоми но приятными плюшками.
    2. Проверка: если расходов нет то секция не рендерится вообще.
    3. при изменени валюты так же меняется сумма категории
    4. если в категории нет расходов то она не кликабельна 
    5. Добалена кнопка сброса на все категории для  блока Евгении

Логику начал делать еще когда небыло реализовано общение с бэком. поэтоу написал такую конструкцию как configs которая является некой базой для последующего динамического содоздания лишек в отрисовке.  